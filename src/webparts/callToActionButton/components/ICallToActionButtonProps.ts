export interface ICallToActionButtonProps {
  buttonText: string;
  buttonShape: string;
  buttonColor: string;
  buttonFontColor: string;
  animation: string;
  redirect: string;
  openInNewWindow: boolean;
}
